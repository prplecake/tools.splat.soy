name: Deploy

on:
  push:
    branches: [ master ]

jobs:
  deploy:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build
      run: make build

    - name: Deploy
      uses: peaceiris/actions-gh-pages@v3.7.3
      with:
        publish_dir: ./_site
